<p style="color: green"><%= notice %></p>

<%= render @resource %>

<div>
  <%= link_to "Edit this project", edit_project_path(@resource) %> |
  <%= link_to "Back to projects", projects_path %>

  <%= button_to "Destroy this project", @resource, method: :delete %>
</div>
